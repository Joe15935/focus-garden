# 上游工作流存档

这些文件来自 Focuser 基础提交，用于保留实现参考。Focus Garden 当前只验证 Apple Silicon macOS；上游多平台构建、浏览器扩展打包及 Focuser 自动发布流程不适用于本次源码公开。

它们位于 `docs/`，GitHub Actions 不会执行。当前验证方式为仓库中的 `scripts/check.sh` 和 `scripts/build-macos.sh`。尚未宣称有通过的 Focus Garden 云端 CI 或经公证的公开安装包。
