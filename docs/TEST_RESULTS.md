# 本机验收记录

验收进行中。此文档在实际 `.app` 安装与运行后补全，不代表已可日常使用。

## 已完成的自动化验证

- Rust workspace：243 个测试通过（含 1 个文档测试）；后续实例锁和额度修复待最后复验。
- Rust workspace clippy：通过。
- 前端：16 个文件，194 个测试通过；类型检查和 production build 通过。
- npm production audit：0 个漏洞（依赖审计不是全面安全保证）。
- 许可证清单、680 个归档文件哈希检查通过；精确版本许可文本来源缺口已在清单如实标注。

一次前端测试与 npm ci 重叠导致依赖文件暂时缺失；停止该次运行，安装完成后重新运行，194 个测试全部通过。第一次 release 链接发现 Swift 静态库内部化符号，正在本项目 vendor 补丁修复。上述失败不记作通过。

## 实机验收

待记录。未测试的项目不会标为 PASS。
