# 本机交付验收记录

记录日期：2026-09-21，America/New_York；本地时间截至 05:05。证据 JSON 的事件时间使用 UTC，本文已换算为本地时间。

**21 项功能门：20 PASS / 1 FAIL。唯一未完成的整门验收是实际 Mac 重启。** PASS 仅覆盖本表明确列出的实机行为，不能解释为所有长期场景均已验证。当前交付没有宣称整机重启、长期稳定性和全部成长解锁已经完成实机验收。

## 范围、版本与证据

- 系统：macOS 26.6.2，Apple Silicon arm64。
- 已安装应用：`/Users/gls/Applications/Focus Garden.app`。
- 最终正式打包产物：`/Users/gls/Projects/focus-garden/target/release/bundle/macos/Focus Garden.app`。
- 本机数据：`/Users/gls/Library/Application Support/Focus Garden/`，使用独立规则和花园 SQLite 数据库。
- [最终自动化检查日志](../evidence/focus-garden-final-acceptance.txt)与[第四次正式构建日志](../evidence/focus-garden-bundle-4-verified.txt)。
- [最终安装核验](../evidence/final-installation-check.json)：记录测试数、签名验证退出码、安装路径、二进制摘要、测试规则关闭、登录启动开启和系统重启未执行。
- [最终 GUI JSON 导出（04:53:27）](../evidence/专注花园-20260921-045327.json)：中文原生保存对话框实际保存，读取及解析成功；包含恢复后的设置、花园与屏蔽列表。
- [全局拦截及额度记录](../evidence/final-block-and-quota.json)：8 条真实事件，含 2 次应用隐藏、6 次网站导航，以及网站额度实际累计 60 秒。
- [较早 JSON（01:51:29）](../evidence/专注花园-20260921-015129.json)、[活动会话 JSON（01:28:39）](../evidence/专注花园-20260921-012839.json)与[CSV（01:32:27）](../evidence/专注花园-20260921-013227.csv)保留各自时点的真实状态，不因后续设置恢复或新增事件而改写。

视觉和操作结论来自主任务完成的安装包实机观察。本次报告整理只读取现有日志与导出，没有操作 UI，没有修改程序、数据、签名或系统权限。

## 自动化与正式构建

以下结果对应最终修复版；正式构建后安装、授权及复测期间未再修改运行时代码。

| 检查 | 结果 | 实际证据及范围 |
|---|---|---|
| Rust 格式检查 | PASS | `./scripts/check.sh` 中 `cargo fmt --all -- --check` 完成 |
| Rust 静态检查 | PASS | `cargo clippy --workspace -- -D warnings` 完成 |
| Rust workspace 测试 | PASS | 最终日志各测试组共 **250 passed / 0 failed**，包含文档测试 |
| 前端类型检查 | PASS | `npm run typecheck` 完成 |
| 前端测试 | PASS | `npm test`：**19 个测试文件、207 passed / 0 failed** |
| 前端生产构建 | PASS | `npm run build` 完成；保留超过 500 kB 的代码块体积提示，最大块约 740.28 kB、gzip 后约 232.95 kB |
| 最终完整 `.app` 构建 | PASS | `./scripts/build-macos.sh` 整段重新运行成功；第四次正式构建日志包含 `Finished 1 bundle` 和实际 `.app` 路径，最终安装核验记录脚本退出码 0 |
| 构建产物及安装包签名 | PASS | 最终安装核验中 artifact 与 installed 的整包 `codesign` 验证退出码均为 0；这是本机临时签名，未公证 |

自动化合计 **457 项测试通过**，与下表 21 项功能验收门是不同口径。执行使用进程内 `DEVELOPER_DIR=/Library/Developer/CommandLineTools` 和 `MACOSX_DEPLOYMENT_TARGET=13.0`，没有改变系统 Xcode 选择。

保留此前失败及其处理，避免把旧失败改写为当时成功：

- 较早一次前端测试与依赖安装重叠，因依赖文件暂时缺失失败；安装完成后重新检查，最终 207 项通过。
- 较早发布构建因 SwiftRs 两个 C 导出被内部化而链接失败；独立项目 vendor 构建补丁修复后，正式构建通过。
- 构建脚本曾多返回一级目录，导致签名步骤找不到 `.app`；修正路径后完整重跑 `./scripts/build-macos.sh` 成功，并重新核验产物及安装包签名。
- 01:59 的应用限制曾实际隐藏成功，却误报失败且不计数。修复后重建、安装，并分别对 Chrome 和 Calculator 实机复验成功，见事件 4、5。

## 21 项功能验收门

PASS 表示该门所列核心行为已有实机观察，并按需要由自动化边界测试补充；每行同时列出实际覆盖范围。未覆盖的扩展场景仍保留，不把短会话、源码存在或权限授予等同于长期使用结果。

| # | 功能门 | 结果 | 已确认的证据与当前边界 |
|---|---|---|---|
| 1 | 安装 Installation | PASS | 最终 `.app` 已安装到 `~/Applications/Focus Garden.app` 并实际打开；安装包与构建产物整包签名验证均成功 |
| 2 | 启动 Launch | PASS | 安装包可操作主页、设置、成就与统计；04:52 正常退出后重开成功，历史与权限保持 |
| 3 | 简体中文 Chinese | PASS | 实际审视中文主页、花园、设置、成就、统计、退出流程及原生菜单；原生保存对话框标签为中文，并成功保存最终 JSON |
| 4 | Pomodoro | PASS | 实际完成 **60 秒专注**及随后 **60 秒休息倒计时**；导出记录 `planned_secs=60 / elapsed_secs=60 / status=completed`。完整 25/50/90 分钟长时会话未逐项实测 |
| 5 | App Block | PASS | 修复版真实隐藏 Chrome（**04:41:54.850833，id=4**）及 Calculator（**04:42:09.025056，id=5**），且数据库计数成功；04:52 重开后统计 UI 显示 2 次应用拦截。隐藏窗口不终止目标进程。Chrome 测试规则已移除 |
| 6 | Website Block | PASS | 三浏览器均有真实 HTTP 页面跳本地中文拦截页及数据库事件：Safari **04:47:22.283092 / id=6**、Chrome **04:48:00.284220 / id=7**、Edge **04:48:13.730848 / id=8**。Safari 首次访问本地 HTML 需要用户确认文件；确认后第二次访问已自动拦截，未外推为重启后永不提示 |
| 7 | Weekly Schedule | PASS | 01:53:49 的真实 Chrome `example.com` 拦截由**周一 01:00–02:00**周计划命中，跳本地页并记 `id=1`。跨到窗外后的自动解除未单独完成实机观察 |
| 8 | Daily Allowance | PASS | `example.net` **60 秒网站额度**完成闭环：前台累计 22 秒，切到其他 App 暂停，再回前台到 60 秒；**01:57:00.663764 / id=2** 跳本地页，UI 显示 1 分钟用完。UI 停用额度与列表、数据库确认 `enabled=0` 后，新 Chrome 标签正常打开 Example Domain。App 每日额度未另行跑满 1 分钟；额度逻辑有单元测试，跨日重置尚未实机观察 |
| 9 | Strict Focus | PASS | 实机“专注”档提前结束申请，等待 30 秒冷静期并填原因后退出；申请 `01:32:18`、结束 `01:33:08`，有效 56 秒、0 XP、中断。后台拒绝过早退出和重复申请不重置冷静期另有测试；系统恢复出口仍保留 |
| 10 | Emergency Override | PASS | 实机“深度专注”申请紧急退出，等待 5 分钟、输入规定确认文字并填写原因后结束；申请 `01:25:15`、结束 `01:31:30`，有效 380 秒、6 XP、中断及原因均持久化。此项不表示系统退出出口已被封锁 |
| 11 | XP | PASS | 真实记录合计 **7 XP**：60 秒完成得 1，380 秒中断得 6，56 秒中断得 0；没有按计划 900 秒发整段奖励。完成奖金与防重复结算另有自动化测试 |
| 12 | Level | PASS | 主页真实显示 **Lv.1 / 7 XP**，与导出一致；等级阈值和越级装扮拒绝有自动化覆盖。**真实跨等级升级及 Lv.3/Lv.6 解锁未实测** |
| 13 | Streak | PASS | 测试时 UI 门槛 1 分钟，完成后实际显示连续 1 天；恢复默认 30 分钟后，最终导出当前/最长连续均为 0，符合本日未达 30 分钟。跨日边界有注入时钟测试，未连续数日实测 |
| 14 | Achievements | PASS | 实机看到 **26 个成就、4 个已获得**；导出为 `first / early / focus / task_done`，均对应真实记录。尚未逐一实测全部长期成就 |
| 15 | Garden | PASS | 实机看到与真实约 3–7 分钟记录关联的种子，以及仍锁定的 25/50/90 分钟预览；未把预览当成已获得成果。成熟植物、高等级装饰未通过真实长时专注逐一验证 |
| 16 | Heatmap | PASS | 实际可见热力图；**7 / 30 / 90 / 365** 范围按钮及格子数量已核对。当天数据来自真实会话，没有伪造全年分布 |
| 17 | Statistics | PASS | 修复版统计页实机正常；04:52 重开后显示 **8 分钟、1 完成、3 中断、2 次应用及 6 次网站拦截**。有效专注精确值为 496 秒，拦截总量与全局事件表一致；导出字段范围差异见下文 |
| 18 | Reboot Persistence | **FAIL** | **未完成实际 Mac 重启验证**。应用退出/重开、历史保留、进行中会话安全中断和登录项注册均不能替代系统重启；实际登录自启也仍待验证 |
| 19 | Dark Mode | PASS | 深色、浅色均已切换并通过实际截图审视；最终恢复“跟随系统”，导出 `theme=system` |
| 20 | Local Data | PASS | 本机专属目录及 SQLite 实际存在；重开应用保留数据，本机使用无需账号。代码审查未引入云数据库/遥测；没有把代码审查当成全环境流量取证 |
| 21 | Export | PASS | GUI 实际保存并解析 JSON、CSV；CSV 含表头及 4 行会话。JSON 包含 `garden` 与 `block_lists`，**不是含所有额度和全局事件的全量备份**；未宣称已验证一键完整恢复 |

## 实机事件与恢复记录

以下 8 条事件均可在[最终全局记录](../evidence/final-block-and-quota.json)中核对；浏览器归属来自对应实机操作，数据库网站事件本身只记录目标网址。

| 本地时间 | 事件 ID | 实际观察 |
|---|---|---|
| 01:53:49.328592 | 1 | Chrome `example.com` 命中周计划，跳本地阻断页 |
| 01:57:00.663764 | 2 | Chrome `example.net` 网站额度累计到 60 秒，跳本地阻断页 |
| 01:58:39.817359 | 3 | Edge `example.com` 跳本地阻断页 |
| 04:41:54.850833 | 4 | Chrome 应用窗口隐藏，数据库成功计数 |
| 04:42:09.025056 | 5 | Calculator 应用窗口隐藏，数据库成功计数 |
| 04:47:22.283092 | 6 | Safari 在首次文件确认之后，再次访问 `example.com` 自动跳本地页 |
| 04:48:00.284220 | 7 | 最终版 Chrome `example.com` 跳本地阻断页 |
| 04:48:13.730848 | 8 | 最终版 Edge `example.com` 跳本地阻断页 |

- **权限保持**：用户已为最终版授予辅助功能及 Safari/Chrome/Edge 自动化权限。04:52 正常退出/重开后，健康检查 `AX=true`，三个浏览器均为 granted；这是进程重开观察，整机重启后仍需验证。
- **Safari 首次文件确认**：首次本地页导航时出现“确认要加载的文件”，用户选择本应用单个 `focus-garden-blocked.html` 后，再次网站命中自动跳转成功。不能承诺 Safari 或系统重启后永不再次要求确认。
- **早期隐藏误报**：01:59:48.079 Chrome 为 `active=true / hidden=false`，至 `.183` 已隐藏、Focus Garden 前台，但界面误报“应用尚未隐藏，未计入拦截次数。”且数据库未计数。该次不能算成功闭环；修复最终状态判断后，以事件 4、5 重新验收。
- **登录启动**：早期在 GUI 开启后 LaunchAgent 出现、关闭后消失，注册及撤销均已验证。**最终 04:53 再开启并保留 ON**；`~/Library/LaunchAgents/Focus Garden.plist` 经 `plutil` 核对，目标为已安装应用的 binary，带 `--autostart`。该参数的启动实现会隐藏主窗口；实际系统登录启动尚未执行。
- **进程重启恢复**：早期进行中的会话重开后标为 interrupted，离线时段没有补发计划时长；最终正常退出/重开后，历史、统计和权限保持。此项不替代整机重启门。
- **停用后补充观察**：05:02–05:03 两轮共 20 次独立系统采样显示 Chrome `hidden=false`，事件数保持 8，所有列表及额度仍停用；但 Chrome `active=false`，不能据此宣称持续前台解除测试通过。先前 Calculator 在停用后的采样仍为隐藏状态，原因未确认，未计为恢复 PASS。[原始记录](../evidence/app-hide-before-fix.txt)保留上述限制。本次创建的 Chrome、Edge、Safari 测试标签和 Finder 测试窗口已关闭。
- **恢复出口**：30 秒与 5 分钟应用内退出流程已实际完成。原生 Dock“退出”和系统强制退出仍是恢复出口，程序关闭后限制不再执行；这些出口不会把未完成计划结算为已完成奖励。

## 最终数据、设置与导出范围

[04:53:27 最终 GUI JSON](../evidence/专注花园-20260921-045327.json)包含 **496 秒有效专注、7 XP、Lv.1、1 次完成、3 次中断、4 个已解锁成就**。0 秒恢复中断不贡献热力图时间，因此当天有时间记录的会话数为 3，与总计 4 条会话并不矛盾。

最终设置已通过 UI 保存、数据库只读核对，并由最终 JSON 再次确认：`streak_minutes=30`、`theme=system`、`daily_goal_minutes=120`、`weekly_goal_minutes=600`；当前及最长 streak 均为 0。较早导出的 1 分钟门槛和深色主题属于当时真实测试状态，旧证据未被改写。

所有测试列表及 `example.net` 额度已关闭，Chrome 的应用测试规则已移除；Calculator 与 `example.com` 规则仍保存在关闭的列表内。登录启动最终保留开启。这些状态区分配置留存与限制正在生效。

最终 GUI JSON 的 `garden.app_blocks`、`garden.website_blocks` 均为 0，因为这些字段统计 **FocusSession 期间**的拦截。本轮 8 次发生于周计划、临时常开或每日额度场景，记录在规则数据库全局事件表；[独立全局事件证据](../evidence/final-block-and-quota.json)及统计 UI 均为 **2 app + 6 website**，不是数据丢失。

GUI JSON 是花园与屏蔽列表的可读归档，不包含所有每日额度配置、用量及全局事件；CSV 是会话表。完整目录备份方法见[数据与恢复](数据与恢复.md)，本阶段未验证从该备份完成整机迁移或一键恢复。

## 资源、签名与系统状态

[资源短样本](../evidence/focus-garden-resource-signature-check.txt)记录 3 个各 5 秒的空闲样本：主进程 CPU **0.0% / 0.1% / 0.2%**，RSS **122,448 KiB，约 119.6 MiB**。只包含主进程，未加总 WebKit/其他辅助进程，不能外推为数小时功耗或所有场景占用。

[最终安装核验](../evidence/final-installation-check.json)记录构建产物及安装包整包签名验证均为退出码 0，授权完成后二进制未变。该本机临时签名未公证；签名成功不等同于权限、拦截或系统重启验收。

最终 hosts 摘要与测试前一致。本任务没有执行修改系统代理的操作，应用运行时代码也不写系统代理；但 `scutil --proxy` 的最终原始输出摘要与初始摘要不同，原因未确认，**不宣称代理快照全程未变**。没有为了验收恢复或改动用户当前代理。

## 尚未完成与已知限制

1. **实际 Mac 重启未进行，Reboot Persistence 为 FAIL。** 登录项配置已保留 ON，但真正登录自启、整机重启后的权限与拦截仍需实测。
2. Safari 首次本地 HTML 导航需要用户确认文件；确认后本机重复拦截成功，重启 Safari 或系统后的提示行为未验证。
3. 应用限制针对前台目标并暂时隐藏窗口，网站限制针对受支持浏览器的活动页面；不阻断后台标签网络/音频，不终止目标进程，不是系统级网络防火墙。程序运行且权限可用时才执行限制；原生 Dock 退出、系统强制退出和撤销权限仍能恢复使用。
4. 完整 25/50/90 分钟会话、真实等级升级、成熟植物、高等级装饰、全部长期成就和连续数日记录未逐一实测。注入时钟测试覆盖边界，不等同于真实等待。
5. 网站每日额度已完成实际到额、切离暂停及停用恢复闭环；App 每日额度未另行跑满 1 分钟，额度跨日重置、计划窗外解除尚无单独实机观察。
6. 数小时连续运行、实际睡眠/唤醒及所有子进程总资源消耗尚无充分证据；本报告只保留短样本结论。
7. GUI JSON/CSV 导出不是全量数据库备份，完整恢复未实测。前端代码块体积提示和许可材料中的精确版本全文缺口仍保留，后者见 [THIRD_PARTY_NOTICES](../THIRD_PARTY_NOTICES)。

当前交付结论：**自动化 457 项通过，完整正式构建及安装包验证通过；功能门 20 PASS / 1 FAIL，整机重启验收待完成。**
